.main-routes {
    animation faded 1s ease alternate
    &.dark{
        div, button,h1,h3,h2,h4,h5,tr,td,thead,tbody,ul,li,section,input, .logo{
            background #292a36 !important;
            color white !important
        }
    }
}
body{
    background-color: rgba(231, 241, 255, 0.3);

    min-height  100%;
    //padding-bottom 50px;
}